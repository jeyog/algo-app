import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: '#006ba6',
      dark: '#004a74',
      light: '#3388b7'
    },
    secondary: {
      main: '#F2CE00',
      dark: '#a99000',
      light: '#f4d733'
    }
  }
})

export default theme